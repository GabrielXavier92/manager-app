schema: http://localhost:4000
config:
  scalars:
    DateTime: Date
generates:
  ./src/types/types.d.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      enumsAsTypes: true